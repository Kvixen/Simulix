cmake_minimum_required(VERSION 3.0)
project(hello-vsc C)
file(GLOB_RECURSE SOURCES_NORMAL ${{CMAKE_SOURCE_DIR}} "{folderName}/*.c")
file(GLOB_RECURSE SOURCES_MATLAB ${{CMAKE_SOURCE_DIR}} "{matlabVersion}/*.c")
file(GLOB_RECURSE SOURCES_INCLUDES ${{CMAKE_SOURCE_DIR}} "includes/*.c")
list(REMOVE_ITEM SOURCES_MATLAB ${{CMAKE_SOURCE_DIR}}/{matlabVersion}/rtw/c/src/common/rt_main.c)
set(SOURCE main.c)

include_directories(${{CMAKE_SOURCE_DIR}}/{folderName}/{modelName}_grt_rtw)
include_directories(${{CMAKE_SOURCE_DIR}}/{matlabVersion}/extern/include)
include_directories(${{CMAKE_SOURCE_DIR}}/{matlabVersion}/rtw/c/src)
include_directories(${{CMAKE_SOURCE_DIR}}/{matlabVersion}/simulink/include)
include_directories(${{CMAKE_SOURCE_DIR}}/includes)
add_executable(main ${{SOURCE}} ${{SOURCES_NORMAL}} ${{SOURCES_MATLAB}} ${{SOURCES_INCLUDES}})